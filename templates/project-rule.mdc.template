---
description: "{{PROJECT_NAME}}: контекст проекта, принятые решения и стандарты работы агента"
alwaysApply: true
---

# {{PROJECT_NAME}} — правила проекта

{{PROJECT_SCOPE}}

## Перед любой задачей

1. Прочитать `{{DOCS_ANCHOR}}` (главный якорь контекста для агента).
2. При изменении поведения — сверить с changelog, architecture, coding-style в docs проекта.
3. Для нетривиальных доработок — следовать `sdd-workflow.mdc` (фазы 0–2 минимум).
4. Перед правкой логики — `change-impact-analysis.mdc`; при ревью — linkage integrity в `code-review-checklist.mdc`.

## Архитектурные границы

<!-- Опишите слои: UI / сервер / провайдеры / интеграции. Что запрещено смешивать. -->

## Стандарты кода (проектные дополнения)

<!-- Только то, что отличается от глобальных rules (1c-coding-standards, anti_patterns). -->
<!-- Общие BSL-правила — в глобальном kit, не дублировать здесь. -->

## Как тестировать

<!-- Сценарии проверки после изменений. -->

## Не делать без запроса

<!-- Явные ограничения scope. -->

## Критичные цепочки (регрессия)

<!-- 3–5 end-to-end сценариев проекта: что обязательно проверить после правки смежного кода. -->

## Верификация

После изменений: линтер/синтаксис; сценарии из раздела «Как тестировать» и «Критичные цепочки»; при сомнениях — `agent-verification-patterns.mdc`, skill `change-impact-review`.

## Knowledge Feedback (после нетривиальных ошибок)

Если ошибка ушла после нескольких итераций и причина неочевидна — см. `knowledge-feedback-loop.mdc`:

1. Записать симптом / причину / fix в `docs/changelog.md`.
2. Проверить, нужно ли дополнить rules/skills в `1c-cursor-kit`.
3. **Спросить пользователя** перед любой правкой kit. Без «да» — только предложение или `/kit-feedback`.
